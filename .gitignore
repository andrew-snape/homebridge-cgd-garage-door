/dist

/.vscode

/node_modules

.DS_Store

# Homebridge runtime state generated by `npm run watch` (accessories/,
# persist/, etc). config.json/auth.json are tracked anyway via `git add -f`.
/test/hbConfig
