{
  "pluginAlias": "CGDGarageDoor",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "schema": {
    "type": "object",
    "required": ["name", "deviceHostname", "deviceLocalKey"],
    "additionalProperties": false,
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "CGDGarageDoor"
      },
      "deviceHostname": {
        "title": "Device Hostname",
        "type": "string"
      },
      "deviceLocalKey": {
        "title": "Device Local Key",
        "type": "string"
      },
      "enableCamera": {
        "title": "Enable Camera",
        "type": "boolean",
        "default": true,
        "description": "Expose the door camera's live view and snapshots in HomeKit."
      },
      "videoProcessor": {
        "title": "Video Processor Path",
        "type": "string",
        "description": "Path to an ffmpeg binary. Leave blank to use the bundled ffmpeg-for-homebridge binary, falling back to 'ffmpeg' on your system PATH."
      },
      "enableVacationSwitch": {
        "title": "Enable Vacation Mode Switch",
        "type": "boolean",
        "default": true,
        "description": "Vacation Mode locks out the door's wireless remote transmitters (handheld clickers), so it won't respond to them until you turn it back off — useful when you're away and want to rule out a lost or cloned remote as a way in. The door can still be opened from HomeKit/Siri or the CGD app while it's on. Disable this to hide the switch tile in the Home app."
      },
      "vacationSwitchName": {
        "title": "Vacation Mode Switch Name",
        "type": "string",
        "default": "Vacation Mode",
        "description": "Customize this if you have another accessory named \"Vacation Mode\" in the same home."
      },
      "enableStopSwitch": {
        "title": "Enable Stop Switch",
        "type": "boolean",
        "default": true,
        "description": "A momentary button that stops the door mid-travel. Disable this to hide the switch tile in the Home app."
      },
      "stopSwitchName": {
        "title": "Stop Switch Name",
        "type": "string",
        "default": "Stop",
        "description": "Customize this if you have another accessory named \"Stop\" in the same home."
      },
      "transitionTimeoutSeconds": {
        "title": "Transition Timeout (seconds)",
        "type": "integer",
        "default": 60,
        "description": "How long (in seconds) the door can stay in a transitional state (Opening or Closing) before the plugin considers it stuck and resets the displayed state to Stopped. Increase this if your door takes longer than 60 seconds to travel. Set to 0 to disable the watchdog."
      }
    }
  }
}
